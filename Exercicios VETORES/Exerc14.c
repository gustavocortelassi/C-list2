// Suponha um vetor N com 10 elementos e outro vetor M com 10 elementos. Faça um
// algoritmo que calcule o produto escalar P de A por B. (Isto é, P = A[1]*B[1] +
// A[2]*B[2] + ... A[N]+B[N]).

#include <stdio.h>
#include <time.h>

int main(){

    srand(time(NULL));

    int vetN[10], vetM[10], result[10];
    int i;

    for (i = 0; i < 10; i++){
        result[i] = vetM[i] 
    }
    

}